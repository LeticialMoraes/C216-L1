/** Rotas da SPA (nomes e URLs em inglês). */
export const paths = {
  home: "/",
  register: "/register",
  login: "/login",
  dashboard: "/dashboard",
  reports: "/reports",
  categories: "/categories",
  suppliers: "/suppliers",
  products: "/products",
  movements: "/movements",
} as const;
