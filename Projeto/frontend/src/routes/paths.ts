/** Rotas da SPA (nomes e URLs em inglês). */
export const paths = {
  home: "/",
  register: "/register",
  login: "/login",
  categories: "/categories",
  suppliers: "/suppliers",
  products: "/products",
} as const;
